#include <stdio.h>
/**
 * main - to print the output
 * Return: 0 shows success non zero shows error
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
